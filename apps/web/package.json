{
  "name": "@gama/web",
  "private": true,
  "version": "0.1.0",
  "description": "Gama web lane for landing, waitlist, and future SEO or content surfaces.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ../../jest.config.cjs --runInBand --testPathPatterns=apps/web",
    "verify": "pnpm lint && pnpm typecheck && pnpm test"
  },
  "dependencies": {
    "@gama/core-domain": "workspace:*",
    "@gama/schemas": "workspace:*",
    "@gama/ui-web": "workspace:*",
    "next": "16.2.2",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "postcss": "8.5.8",
    "tailwindcss": "4.2.2"
  }
}
