{
  "name": "@gama/core-domain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared domain rules for Safe-to-Spend, reimbursements, event intelligence, and forward-looking financial decisions.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ../../jest.config.cjs --runInBand --testPathPatterns=packages/core-domain"
  }
}
