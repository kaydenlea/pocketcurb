{
  "name": "@gama/schemas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared validation schemas and typed contracts for Gama.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ../../jest.config.cjs --runInBand --testPathPatterns=packages/schemas"
  },
  "dependencies": {
    "@gama/core-domain": "workspace:*",
    "zod": "^3.24.2"
  }
}
