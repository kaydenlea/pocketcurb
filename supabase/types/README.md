# Supabase Types

This folder is reserved for generated artifacts from the Supabase CLI or CI.

Current rule:

- generate raw database types here first
- review them
- then promote stable shared types into `packages/supabase-types`

Do not treat generated types as permission to skip schema or RLS review.
